<h1><%= @staff.name %></h1>

<%= render('staff/data') %>


<table class="table table-striped search-table">
  <thead>
    <tr>
      <th>Contacts this staff made</th>
    </tr>
  </thead>
  <tbody>
    <% @contacts.each do |contact| %>
    <tr>
        <td><%= link_to("/contact/#{contact.id}") do%><%= contact.contact_day %> | <%= contact.customer %> <% end %></td>
    </tr>
    <% end %>
  </tbody>
</table>